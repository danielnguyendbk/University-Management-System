spring.application.name=studentportal

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/merge_migration?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
spring.datasource.username=daniel
spring.datasource.password=Thai06112005
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT (sau này dùng)
jwt.secret=diem_secret_key_dai_it_nhat_32_ky_tu_nhe
app.jwt.expiration=3600000

# Server port
server.port=8080

# Enable debug logging for auth troubleshooting
logging.level.com.ptit.studentportal.auth=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.com.ptit.studentportal.timetable=DEBUG

# Multipart config
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# SePay Webhook Configuration
sepay.webhook-api-key=PTIT_Tuition_Secret_2026

# ============================================================
# DEMO / DEV ONLY — DO NOT SET demo-amount-enabled=true IN PRODUCTION
# When true, a transfer of exactly demo-amount VND (e.g. 2,000) is
# accepted as full payment for local demo / committee review purposes.
# This bypasses the real amount check ONLY when enabled here.
# ============================================================
payment.demo-amount-enabled=true
payment.demo-amount=2000

# Email Configuration - SendGrid SMTP
spring.mail.host=smtp.sendgrid.net
spring.mail.port=587
spring.mail.username=apikey
***REMOVED***${SENDGRID_API_KEY:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Application settings
app.mail.from=ptithcmdemo@gmail.com
app.mail.mode=smtp
app.frontend.baseUrl=http://localhost:5173
